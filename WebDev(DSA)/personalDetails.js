const name='Yash Prasad'
const age=24
const address='bangalore'

const persnoalDetails=`My name is '${name}', age ${age}, and my address is '${address}'.`;
console.log(persnoalDetails)

console.log(persnoalDetails.split(','))